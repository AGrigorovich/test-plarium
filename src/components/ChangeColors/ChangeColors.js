import React, { useState } from 'react';

import './ChangeColors.css';

const ChangeColors = () => {
	const [isMenuOpen, changedMenuVisibility] = useState(false);
	return <div className="color-picker-rows">C</div>
};

export default ChangeColors;
